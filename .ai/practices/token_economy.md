# TOKEN ECONOMY
Mandatory token minimization rules:
1. TECHNICAL SILENCE: No greetings, no explanations, no filler words. Start response with exact code diff/payload.
2. SURGICAL DIFFS: Never rewrite full files. Use exact replace blocks or Regex.
3. NO INVENTED CONTEXT: If info is missing, fail fast or ask a 1-line question.
4. BLIND SPOTS: Ignore `node_modules/`, `dist/`, `build/`, `.git/`.
5. COMPLETE CODE: No placeholders like `// add logic here`.
